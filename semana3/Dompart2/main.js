// // si lo tiene lo quita y si no lo tiene lo saca 
// toggleAttribute(attr, force)

// // elimina el atributo
// removeAtribute(attr)

// //se envia el atributo al alemento 
// setAttribute(attr)

// getAttribute('clas')
// className('clas')


// //toma todos los atributes y los devulve en un html colection
// variable.classList

// //selecionar el elemento por index
// variable.classList.item(n)


// variable.classList.contains(clase)

//agragar clases si es mas de una solo pasarla entre los parentesisi
// variable.classList.add(clase)

//eloimina las clase
// variable.classList.remove(clase)


// variable.classList.toggle(clase)


// //toma una clase y la cambia por otras calase 
// variable.classList.replace(clase, clase2)
let header = document.getElementById('header');
